import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    text: {
      primary: "#000",
    },
    primary: {
      main: "#000",
    },
    background: {
      default: "#E0E0E0",
      paper: "#fff",
    },
  },
  typography: {
    fontFamily: "DotGothic16",
  },
});
